# Word-Count

Overview Sort through a list of files in a folder structure

Requirements

Go through all files inside a folder and its subfolders.
Extract words from file names (not from inside the files).
Count how often each word appears across all file names (ignoring case).
Only report words that appear more than twice and sort them from most to least frequent.

