# Word-Count

Overview Sort through a list of files in a folder structure

Requirements

1- Go through all files inside a folder and its subfolders.
2- Extract words from file names (not from inside the files).
3- Count how often each word appears across all file names (ignoring case).
4- Only report words that appear more than twice and sort them from most to least frequent.

